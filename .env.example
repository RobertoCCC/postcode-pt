# Copy to .env and adjust for local dev.
# .env is gitignored — never commit secrets.

# Async SQLite for local dev (default).
# Migrate to Postgres for production:
# DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/postcode_pt
DATABASE_URL=sqlite+aiosqlite:///./postcode_pt.db
